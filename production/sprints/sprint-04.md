# Sprint 4 — 2026-04-03 to 2026-04-09

**Status**: Not Started
**Last Updated**: 2026-04-03

## Sprint Goal

Close all remaining M2 exit criteria: implement the Obstacle System, SFX and Music
Managers, redesign w1_l4–w1_l6 with route forks, add two obstacle-showcase levels,
and fix Bug #10 (World Map title centering) — delivering a complete jam-submission-ready build.

## Capacity

- Total days: 5 (Fri Apr 3 – Thu Apr 9)
- Buffer (20%): 1 day reserved for integration issues and playtest validation
- Available: 4 net days

> **Velocity note**: Sprint 3 retro (Action Item #1) recommends abandoning the 16-task
> time-boxed model in favour of milestone-driven continuous delivery. Three sprints confirm
> actual delivery is ~0.025d/task; this sprint uses a **0.05d/task estimate** (2× safety
> buffer) and plans 26 tasks representing all remaining M2 work. Historical pattern projects
> completion in <1 calendar day; the 5-day window provides full M2 buffer re: April 14
> target.

---

## Tasks

### Must Have (Critical Path)

| ID    | Task                                                                                                                                                                                                                                                                                                                                                                                                                       | Agent/Owner         | Est. Days | Dependencies | Acceptance Criteria                                                                                                                                                                                                                                                |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | --------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S4-04 | **ObstacleSystem node**: Create `src/gameplay/obstacle_system.gd` — `initialize_obstacles(level_data, tilemap_layer)` iterates all grid coords, sets `TileMapLayer` cells via `Grid.get_tile_art_id()`, builds `_obstacle_index: Dictionary[Vector2i, ObstacleType]`, emits `obstacle_registered` per entry; `reset()` clears index + TileMapLayer; `set_obstacle_active()` is stubbed with `push_warning()`               | gameplay-programmer | 0.1       | —            | `initialize_obstacles()` renders all floor and wall tiles in a single pass; `_obstacle_index` contains exactly the blocking tiles from the level data; `obstacle_registered` signal fires once per obstacle; `reset()` clears all state; no existing tests regress |
| S4-05 | **ObstacleSystem + SlidingMovement integration**: Confirm `SlidingMovement` already consults `Grid.get_tile(coord).is_walkable()` for blocking checks; if ObstacleSystem only populates visual tiles (not `GridTileData.is_walkable`), ensure `initialize_obstacles()` also calls `Grid.set_tile_walkability(coord, false)` for BLOCKING tiles                                                                             | gameplay-programmer | 0.05      | S4-04        | Cat cannot slide through a BLOCKING obstacle tile; collision stop point matches the visual wall boundary exactly; existing `test_sliding_movement.gd` cases still pass                                                                                             |
| S4-06 | **BFS solver — obstacle awareness**: Verify `tools/level_solver.gd` reads `tile.is_walkable` (not a hard-coded walkability flag separate from obstacle state); if solver uses a separate walkability array, sync it with `Grid.get_tile().is_walkable` post-`initialize_obstacles()`; re-run solver on all 6 existing levels and confirm `minimum_moves` still match                                                       | tools-programmer    | 0.05      | S4-04        | `level_solver.gd` respects obstacle tiles when computing minimum path; running `godot --headless -s tools/level_solver.gd` on `w1_l1`–`w1_l6` produces unchanged `minimum_moves` values (obstacles not yet in those levels); no solver regression                  |
| S4-07 | **Redesign w1_l4 — route fork + star threshold**: Edit `data/levels/world1/w1_l4.tres` to introduce at least one meaningful choice point (a suboptimal path is reachable but the 3-star path requires a deliberate sequence); adjust `star_3_moves` to `minimum_moves + 1` or `+2` if needed; re-run BFS solver to verify `minimum_moves`                                                                                  | gameplay-programmer | 0.1       | S4-06        | w1_l4 has ≥2 viable routes of different lengths; BFS `minimum_moves` re-verified; 3-star threshold is achievable without perfect foreknowledge (threshold ≤ `minimum_moves + 2`); `.tres` file committed                                                           |
| S4-08 | **Redesign w1_l5 — route fork + star threshold**: Same requirements as S4-07 applied to `w1_l5.tres`                                                                                                                                                                                                                                                                                                                       | gameplay-programmer | 0.1       | S4-06        | Same acceptance criteria as S4-07 applied to w1_l5                                                                                                                                                                                                                 |
| S4-09 | **Redesign w1_l6 — route fork + star threshold**: Same requirements as S4-07 applied to `w1_l6.tres`                                                                                                                                                                                                                                                                                                                       | gameplay-programmer | 0.1       | S4-06        | Same acceptance criteria as S4-07 applied to w1_l6                                                                                                                                                                                                                 |
| S4-10 | **New levels w1_l7 + w1_l8 — obstacle showcase**: Author two new `LevelData` `.tres` files (`data/levels/world1/w1_l7.tres`, `w1_l8.tres`) that specifically showcase static `BLOCKING` obstacle tiles; each level must have ≥1 non-trivial route choice; BFS verified; register both in `data/level_catalogue.tres`                                                                                                       | level-designer      | 0.1       | S4-04, S4-06 | Two new level files exist at canonical paths; both are registered in `level_catalogue.tres`; BFS solver confirms `minimum_moves` for both; levels load in gameplay without errors; total level count in catalogue is ≥8                                            |
| S4-11 | **SFX Manager autoload**: Create `src/core/sfx_manager.gd` — `AudioStreamPlayer` pool (size 8); `SFX` and `UI` buses; `play(stream: AudioStream, bus: SfxBus, pitch_scale: float = 1.0)`; graceful null guard (warn + return); volume/mute in `user://settings.cfg`; load settings in `_ready()`; register as autoload `SfxManager` in `project.godot`                                                                     | audio-director      | 0.1       | —            | `SfxManager.play()` routes to correct bus without crashing; passing `null` logs a warning and returns cleanly; `user://settings.cfg` SFX entries survive restart; autoload registered and available before first scene loads                                       |
| S4-12 | **Wire 4 SFX events**: Wire `SfxManager.play()` calls for: (1) `slide_move` in `SlidingMovement` → SFX bus, (2) `level_complete` in `LevelCoordinator` → SFX bus, (3) `star_earned` in `LevelCompleteScreen._populate_results()` → SFX bus, (4) `button_tap` on all HUD buttons → UI bus; use silence/stub `AudioStream` resources for now — no audio files required                                                       | audio-director      | 0.1       | S4-11        | All four call sites compile and run without error; stub streams produce no audio but cause no crash; `SfxManager.play(null, ...)` warning path not triggered at any wired call site                                                                                |
| S4-13 | **Music Manager autoload**: Create `src/core/music_manager.gd` — two `AudioStreamPlayer` nodes for cross-fade; subscribes to `SceneManager.transition_completed(screen)` and `SceneManager.world_changed(world_id)` signals; screen-to-track mapping per GDD §2; `play(stream)` with same-track guard; silence stub for all tracks (no audio file required at MVP); register as autoload `MusicManager` in `project.godot` | audio-director      | 0.1       | —            | `MusicManager` initialises without crash; subscribes to both SceneManager signals in `_ready()`; `play()` with null guard logs warning and returns; autoload registered; `transition_completed` does not crash when no track is assigned                           |

---

### Should Have

| ID    | Task                                                                                                                                                                                                                                                                      | Agent/Owner               | Est. Days | Dependencies | Acceptance Criteria                                                                                                                                                                            |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | --------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S4-14 | **Main Menu scene wiring**: Confirm `src/ui/main_menu.gd` wires `PlayBtn` → `SceneManager.go_to(Screen.WORLD_MAP)` and `SkinsBtn` → `SceneManager.go_to(Screen.SKIN_SELECT)` (stub navigation acceptable); cat sprite shows `SaveManager.get_equipped_skin()` placeholder | ui-programmer             | 0.05      | —            | "Play" button navigates to World Map; "Skins" button navigates without crash (stub screen acceptable); cat sprite displays without SCRIPT ERROR; no hardcoded fallback crashing                |
| S4-15 | **ObstacleSystem GUT tests**: Write `tests/test_obstacle_system.gd` — cover: obstacle index populated correctly, blocking tiles in index, walkable tiles not in index, `obstacle_registered` signal fires, `reset()` clears index, `set_obstacle_active()` stub warns     | godot-gdscript-specialist | 0.05      | S4-04        | ≥6 test cases; all pass; suite total ≥520 passing                                                                                                                                              |
| S4-16 | **SFX Manager GUT tests**: Write `tests/test_sfx_manager.gd` — cover: play with valid stream, play with null (warn + no crash), volume setter persists, mute toggle, pool wraps when full (oldest interrupted), bus routing per enum value                                | godot-gdscript-specialist | 0.05      | S4-11        | ≥6 test cases; all pass; no audio hardware required in headless test                                                                                                                           |
| S4-17 | **Music Manager GUT tests**: Write `tests/test_music_manager.gd` — cover: initialises without crash, `transition_completed` calls `play()` with mapped track, same-track guard (no restart), `world_changed` selects correct world track, null track graceful             | godot-gdscript-specialist | 0.05      | S4-13        | ≥4 test cases; all pass; no audio hardware required in headless test                                                                                                                           |
| S4-18 | **Audio bus setup**: Add `SFX`, `UI`, and `Music` audio buses to `project.godot`; set default volumes to 0dB; confirm `SfxManager` and `MusicManager` reference exact bus name strings matching project config                                                            | audio-director            | 0.05      | S4-11, S4-13 | Three buses visible in Godot Audio panel; `SfxManager` and `MusicManager` bus name constants match project config strings; no `Invalid bus index` errors at runtime                            |
| S4-19 | **Automated playtest — full M2 loop**: Run `PlaytestCapture` through: Main Menu → World Map (verify levels show) → Level w1_l7 → complete → Level Complete screen → World Map return; capture screenshots of each stage; check for crashes                                | qa-tester                 | 0.05      | S4-10, S4-14 | ≥4 screenshots saved to playtest output path covering Main Menu, World Map with 8 levels, gameplay, Level Complete; zero crashes in debug output; World Map star count updated post-completion |
| S4-20 | **Bug #10 — World Map title centering**: Set `NekoDash` title label in `scenes/ui/world_map.tscn` to `size_flags_horizontal = SIZE_EXPAND_FILL` (or equivalent) with `horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER`                                                 | ui-programmer             | 0.05      | —            | "NekoDash" title is horizontally centred regardless of screen resolution; confirmed via playtest screenshot                                                                                    |

---

### Nice to Have

| ID    | Task                                                                                                                                                                                                                                                                    | Agent/Owner               | Est. Days | Dependencies          | Acceptance Criteria                                                                                                                                                                     |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | --------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S4-21 | **Background color fix**: Update `scenes/ui/main_menu.tscn`, `scenes/ui/world_map.tscn`, and `scenes/ui/level_complete.tscn` — replace `Color(0.08, 0.08, 0.12)` near-black background with `Color("#F5EDCC")` cream (design system primary background)                 | ui-programmer             | 0.05      | —                     | All three scenes display cream background; no scenes display near-black; confirmed via playtest screenshot of each scene                                                                |
| S4-22 | **CatSprite asset swap**: In `src/ui/cat_sprite.gd`, replace the GDScript-drawn orange circle with `design/draft/sprite-cat.png` loaded as a `Sprite2D` or `TextureRect`; keep the GDScript draw fallback behind a debug flag                                           | technical-artist          | 0.05      | —                     | Cat sprite renders as the white kawaii cat PNG; no orange circle visible; GDScript draw path disabled by default; fallback guard does not crash if PNG is missing                       |
| S4-23 | **CoverageVisualizer trail color**: Update `src/ui/coverage_visualizer.gd` — replace flat green `Color(0.2, 0.7, 0.3, 0.6)` trail with warm amber `Color("#F5C842", 0.6)` to match design spec; leave paw stamp texture as a TODO comment for future asset pass         | technical-artist          | 0.05      | —                     | Coverage trail is warm amber (not green); existing `test_coverage_visualizer.gd` or visual tests pass; comment documents paw stamp texture as future work                               |
| S4-24 | **SfxLibrary resource scaffold**: Create `data/sfx_library.tres` — a `Resource` class that holds `@export` `AudioStream` properties for the four wired SFX events (`slide_move`, `level_complete`, `star_earned`, `button_tap`); values are null (awaiting audio files) | sound-designer            | 0.05      | S4-11                 | Resource file exists at canonical path; can be loaded without error; null streams trigger `SfxManager`'s graceful null guard warning; structure ready for real audio                    |
| S4-25 | **M2 gate check report**: Run the formal gate-check skill against all M2 exit criteria; produce a PASS/CONCERNS/FAIL verdict document at `docs/m2-gate-check.md`; list any criteria still outstanding with owner and ETA                                                | qa-lead                   | 0.05      | S4-04 through S4-13   | `docs/m2-gate-check.md` exists; verdict is PASS or CONCERNS (no blocking FAIL); all open criteria have a named owner and ETA ≤ 2026-04-14                                               |
| S4-26 | **World Map level cards — visual upgrade**: Replace plain `Button` nodes in the World Map level grid with a `PanelContainer`-based styled card showing: level name, best star count (from SaveManager), lock icon or ✓ icon for unlocked/locked state                   | ui-programmer             | 0.1       | —                     | Level cards match Component 3.7 intent (name + stars + lock state visible per card); no regression in level navigation; card click still triggers `SceneManager.go_to(Screen.GAMEPLAY)` |
| S4-27 | **Settings.cfg SFX + Music volume scaffold**: Confirm `SfxManager` and `MusicManager` both read from and write to `user://settings.cfg` using matching key names (`sfx_volume`, `sfx_mute`, `music_volume`, `music_mute`); confirm no key conflict with save.json       | godot-gdscript-specialist | 0.05      | S4-11, S4-13          | Both managers read settings at startup; no key collision with `user://nekodash_save.json`; tests confirm roundtrip persistence                                                          |
| S4-28 | **GUT suite ≥600 tests milestone**: After all Should Have test files land (S4-15, S4-16, S4-17), check total test count; if below 600, identify the largest regression-risk surface not yet tested and add a targeted file to reach ≥600                                | qa-tester                 | 0.05      | S4-15, S4-16, S4-17   | `godot --headless` GUT run reports ≥600 passing, 0 failing                                                                                                                              |
| S4-29 | **Commit hygiene audit**: After all sprint tasks are committed, run `git log --oneline` and confirm every feat commit contains the correct sprint subtask ID (S4-0X); flag any bundles; open a follow-up task if any subtask ID is missing from commit history          | devops-engineer           | 0.05      | all sprint tasks done | Every `feat:` commit in the sprint log contains exactly one `S4-0X` ID; no task is missing from git history; any exceptions explicitly documented with justification                    |

---

## Carryover from Sprint 3

None. All Sprint 3 tasks completed at sprint close.

| Playtest Bugs Carried Forward  | Source                    | Status                |
| ------------------------------ | ------------------------- | --------------------- |
| Bug #6: World Map empty        | REPORT_PLAYER, 2026-04-02 | ✅ Fixed (pre-sprint) |
| Bug #7: World Map back button  | REPORT_PLAYER, 2026-04-02 | ✅ Fixed (pre-sprint) |
| Bug #8: NEW BEST badge missing | REPORT_PLAYER, 2026-04-02 | ✅ Fixed (pre-sprint) |
| Bug #9: Button order           | REPORT_PLAYER, 2026-04-02 | ✅ Fixed (pre-sprint) |
| Bug #10: Title centering       | REPORT_PLAYER, 2026-04-02 | → S4-20 (Should Have) |

---

## Risks

| Risk                                                                                                                                 | Probability | Impact | Mitigation                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------------------------------------------------ | ----------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R-13: Obstacle System integration — additive API changes to GridSystem/LevelData break existing 503 tests                            | Medium      | Medium | Use additive-only changes: new `obstacle_type` field on `GridTileData`, no changes to `is_walkable()` signature. Run full GUT suite after S4-04 before proceeding to S4-05.                        |
| R-14 (active): Level level redesign (S4-07–09) invalidates BFS-verified `minimum_moves`                                              | Low         | Medium | Re-run `godot --headless -s tools/level_solver.gd` on each revised `.tres` before commit. Do not commit without solver confirmation.                                                               |
| R-16 (active): Audio assets unavailable — SFX/Music Manager compiles but is silent                                                   | Medium      | Low    | Both managers must not crash on null streams; stub AudioStream resources are acceptable; game is submittable without audio content.                                                                |
| R-20 (new): Level redesign of w1_l4–l6 requires more grid-constraint work than estimated — narrow grid leaves few viable fork points | Low         | Medium | Use the gameplay prototype levels as reference for fork design; if a level is infeasible to redesign within its grid size, extend the grid by 1 row/col rather than distorting the obstacle count. |
| R-21 (new): Main Menu "Skins" button targets `Screen.SKIN_SELECT` which has no `.tscn` yet — navigation call crashes                 | Medium      | Medium | Stub the `Screen.SKIN_SELECT` route in `SceneManager` to log a warning and no-op; do not assert-fail or crash; document as MVP-Skins tier deferred to M3.                                          |

---

## Dependencies on External Factors

- `ObstacleSystem` (S4-04) must land before level redesign (S4-07–09) and new levels (S4-10) — level files reference obstacle tile types that the system must be able to render.
- `SfxManager` (S4-11) must land before SFX wiring (S4-12) — cannot reference `SfxManager.play()` before the autoload exists.
- `MusicManager` (S4-13) must land before audio bus setup (S4-18) — bus name constants are defined in S4-13.
- No physical device required this sprint — all validatable in editor or via automated playtest.

---

## Definition of Done for this Sprint

- [ ] All Must Have tasks (S4-04 through S4-13) completed and tests passing
- [ ] Zero SCRIPT ERRORs on full GUT test run
- [ ] `ObstacleSystem` initialises, renders, and blocks movement for BLOCKING tiles
- [ ] BFS solver respects obstacle tiles; all existing `minimum_moves` values confirmed unchanged
- [ ] w1_l4, w1_l5, w1_l6 each redesigned with ≥1 route fork; `star_3_moves` adjusted; BFS re-verified
- [ ] `data/levels/world1/w1_l7.tres` and `w1_l8.tres` exist, registered, BFS verified — total ≥8 levels
- [ ] `SfxManager` autoload live; ≥4 SFX call sites wired; null-guard prevents crashes
- [ ] `MusicManager` autoload live; subscribes to `transition_completed` and `world_changed`
- [ ] M2 milestone exit criteria checklist updated; target PASS by 2026-04-14
- [ ] Each sprint sub-task committed individually as `feat: S4-0X PascalCaseTaskName` (no bundling — S3-05 lesson applied)
- [ ] Sprint retrospective filed within 24h of all-tasks-complete
